<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"> <!-- Thêm Bootstrap CSS -->
    <%- include('../layout/header') %>
    <%- include('../layout/navbar') %>
    <title>Home - Online Pet Shop</title>
</head>

<body>
    <!-- Banner Section -->
    <section class="banner">
        <div class="container text-center">
            <h1>Chào mừng đến với cửa hàng thú cưng của chúng tớ</h1>
            <p>Cửa hàng tổng hợp về tất cả đồ dùng và phụ kiện dành cho thú cưng</p>
            <a href="/products" class="btn btn-primary">Mua ngay nào</a>
        </div>
    </section>

    <!-- Featured Products Section -->
    <section class="featured-products py-5">
        <div class="container">
            <h2 class="text-center mb-4">Featured Products</h2>
            <% if (products.length > 0) { %>
                <div class="row">
                    <% products.forEach(product => { %>
                        <div class="col-md-4 mb-4"> <!-- Sử dụng Bootstrap grid -->
                            <div class="product-card">
                                <a href="/products/<%= product._id %>">
                                    <img src="<%= product.imageUrl %>" alt="<%= product.name %>" class="product-image">
                                </a>
                                <div class="card-body">
                                    <h5 class="card-title">
                                        <a href="/products/<%= product._id %>"><%= product.name %></a>
                                    </h5>
                                    <p class="card-text">Giá: <%= product.price.toFixed(2) %> VND</p>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <p class="no-products text-center">No products found.</p>
            <% } %>
        </div>
    </section>

    <%- include('../layout/footer') %>

    <!-- Thêm Bootstrap JS và jQuery -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap /4.5.2/js/bootstrap.min.js"></script>
    <script src="/js/main.js"></script> <!-- Thêm tệp main.js -->
</body>

</html>